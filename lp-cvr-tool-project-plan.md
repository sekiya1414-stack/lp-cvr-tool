# LP-CVR-Tool 開発計画書 兼 進捗管理表

最終更新: 2026-08-06
リポジトリ: https://github.com/sekiya1414-stack/lp-cvr-tool
公開URL: https://sekiya1414-stack.github.io/lp-cvr-tool/

---

## 1. プロジェクト概要

「デザインツール」ではなく「勝てるLPを自動で見つけるシステム」を目指す、CVR(コンバージョン率)最適化特化のLP自動生成ツール。

- LLM(Claude)で複数の訴求パターン(価格訴求・権威訴求・感情訴求)のLPを生成
- 公開後の実データ(クリック率・CVRなど)をもとに勝ちパターンを検証
- 業種横断で「勝ちパターンDB」を蓄積し、新規案件の初期生成に反映していく

環境方針: PCへの負担を避けるため、GitHub Pages + サーバーレス(Vercel/Cloudflare Workers)+ Supabaseで構成。開発はClaude Code(ターミナル)で実施。

---

## 2. フェーズ構成と進捗ステータス

### フェーズ1: 静的LP + GitHub Pages公開 【✅ ほぼ完了】

| # | タスク | ステータス |
|---|---|---|
| 1-1 | リポジトリ構成設計(lp/, templates/, prompts/, scripts/) | ✅ 完了 |
| 1-2 | git init・ローカルGit設定 | ✅ 完了 |
| 1-3 | GitHubリポジトリ作成・remote連携・push | ✅ 完了(PATにworkflowスコープ追加、資格情報再認証を経て解決) |
| 1-4 | リポジトリをPublicに変更(Pages有効化の前提条件) | ✅ 完了 |
| 1-5 | GitHub Pages設定(Source: GitHub Actions) | ✅ 完了 |
| 1-6 | deploy.ymlによる自動デプロイ動作確認 | ✅ 完了(ビルド・デプロイともに成功) |
| 1-7 | サンプルLP3パターン(variant-1価格訴求 / variant-2権威訴求 / variant-3感情訴求)の公開確認 | ✅ 3variant全URLで疎通確認済み(curlでHTTP 200)。ブラウザ目視確認はvariant-1・variant-2(2026-08-02、スマホ実機で表示崩れなし確認)完了、variant-3は未実施 |
| 1-12 | 本番稼働の最終検証(VPS経由の疎通・Formspreeテスト送信・GoatCounter実データ) | ✅ 完了(2026-08-02。VPSからcurlで3variant全ページHTTP 200、Formspreeへテスト送信2回とも正常受理、GoatCounterで実ブラウザアクセス3件を確認) |
| 1-13 | X投稿によるSNS初回配布 | ✅ 完了(2026-08-02。UTM付きURLでvariant-2をXに投稿、ユーザー報告により確認済み) |
| 1-8 | Canva経由での画像生成フロー検証 | ✅ 完了(候補4案生成→1案選択→Canvaデザインとして保存済み) |
| 1-9 | Canva生成画像をLPのヒーロー画像として差し替え | 🔄 進行中(画像ダウンロード待ち) |
| 1-10 | CTAフォームをFormspreeに接続(送信成功/失敗をその場表示) | ✅ 完了(2026-07-26、エンドポイント: https://formspree.io/f/xrenovaz。fetch+preventDefaultでページ遷移なしの結果表示まで実装済み) |
| 1-11 | GoatCounterによるアクセス解析導入(全ページ) | ✅ 完了(2026-07-26、rojiuracity.goatcounter.comで全variant + トップページに計測タグ追加済み) |

**フェーズ1進捗率: 約97%**(残タスク: Canva画像のヒーロー差し替え反映、variant-3の目視確認のみ)

---

### フェーズ2: バックエンド追加(A/Bテスト振り分け・計測・LLM連携) 【🔄 着手中】

実行環境は **Cloudflare Workers に確定**(2026-09-11、Vercel Hobbyが商用利用不可のため
コスト面でCloudflareを選択)。DBはSupabaseから**Cloudflare D1へ置き換え検討中**(ユーザー最終確認待ち)。

| # | タスク | 概要 | ステータス |
|---|---|---|---|
| 2-1 | Cloudflare Workersプロジェクトのセットアップ | `workers/`にwrangler.toml・package.json新設 | ✅ 完了(2026-09-11) |
| 2-2 | `workers/src/index.ts`内 `/assign` 実装 | 訪問者ごとのバリアント振り分け(Cookie管理含む)、静的LPへ302リダイレクト | ✅ 完了・実機確認済み(2026-09-11) |
| 2-3 | `workers/src/index.ts`内 `/track` 実装 | クリック・CVイベントの計測API(CORS対応) | ✅ 完了・実機確認済み(2026-09-11) |
| 2-4 | 画像生成機能 | Stability AI/DALL-E等での画像生成(Canva運用と並行 or 置き換え検討) | ⬜ 未着手 |
| 2-5 | コピー生成機能 | Anthropic API経由でのコピー生成 | ⬜ 未着手 |
| 2-6 | DB作成・スキーマ設計 | Cloudflare D1採用に確定。`lp-cvr-tool-db`作成、`events`テーブル作成済み | ✅ 完了(2026-09-11) |
| 2-7 | 統計判定ロジック(`workers/src/stats.ts`、`GET /stats`) | Beta-Binomial+モンテカルロでvariantごとの勝率(probabilityBest)・勝者を判定 | ✅ 完了・合成データで動作検証済み(2026-09-11) |
| 2-8 | 既存の静的LPとバックエンドの結合テスト | 振り分け→計測→判定の一連動作確認 | ✅ view/cvイベント送信を組み込み、本番LPで実機確認済み(2026-09-11) |

**フェーズ2進捗率: 約80%**(振り分け・計測・統計判定まで本番で動作確認済み。
配布リンクは`/assign`経由に切り替え可能な状態(独自ドメイン化は見送り、`workers.dev`
URLをそのまま使用する方針に確定・2026-09-11)。残るは画像/コピー生成(2-4/2-5))

**統計API**: `GET https://lp-cvr-tool.lp-cvr-tool-workers.workers.dev/stats?project=project-a`
(各variantのCVR・ベイズ的勝率・95%信用区間・勝者判定をJSONで返す)

**公開URL(Workers)**: https://lp-cvr-tool.lp-cvr-tool-workers.workers.dev

---

### フェーズ3: 勝ちパターンDB蓄積・自動分析 【⬜ 未着手】

| # | タスク | 概要 |
|---|---|---|
| 3-1 | 勝ちパターンDBスキーマ設計(`analysis/pattern-db-schema.sql`) | 業種×訴求軸ごとの勝率蓄積構造 |
| 3-2 | 案件横断データの蓄積運用開始 | 複数案件(目安10〜20件)でのデータ収集 |
| 3-3 | 週次自動分析バッチ(`analysis/weekly-report.ts`) | GitHub Actionsでのcron実行 |
| 3-4 | LLMによる勝因解釈・改善提案の自動生成 | 計測データを要約してClaudeに解釈させる |
| 3-5 | 新規案件への勝ちパターン反映ロジック | 業種類似度に基づく初期生成の重み付け |

**フェーズ3進捗率: 0%**

---

## 3. 現在地点のまとめ

- フェーズ1はほぼ完了し、実際にインターネット上でLPが公開されている状態
- CTAフォームのFormspree接続、GoatCounterによるアクセス解析はすでに実装・公開済み(2026-07-26)
- 2026-08-02にX投稿(SNS初回配布)まで完了。投稿前の本番稼働最終検証(VPS疎通・Formspreeテスト送信・GoatCounter実データ確認)も実施済み
- SNSでのテスト配信は「コピー・デザインへの主観的フィードバック収集」であり、本格的なCVR計測はフェーズ2完了後でないと得られない点に注意
- Canva経由の画像生成フローは検証済みで、次はLPへの反映(差し替え)が直近タスク(未着手)

---

## 4. 次のステップ(直近アクション、優先順)

1. **[配布フォローアップ]** GoatCounterの「キャンペーン」欄で`taskflow-phase1`(X投稿分)の反応を確認する
2. **[配布フォローアップ]** Formspreeの「投稿」一覧で、テストデータ以外の実問い合わせが届いていないか定期チェックする
3. **[フェーズ1仕上げ]** Canvaでダウンロードした画像を `lp/project-a/variant-1〜3/` のヒーロー画像として差し替える
4. **[フェーズ1仕上げ]** variant-3 の表示内容をブラウザ(実機)で確認する
5. **[フェーズ1完了後]** 反応を見て追加チャネルでの配布を検討する
6. **[フェーズ2着手]** Vercel/Cloudflare Workersのセットアップから開始し、`assign-variant.ts`・`track-event.ts` を実装する
7. **[フェーズ2着手]** Supabaseプロジェクトを作成し、イベントログ用テーブルを設計する

---

## 5. 稼働時間試算(暫定値・要更新)

以下は会話内容から見積もった**暫定の目安**です。実際の残作業量・Claude Codeとの実装効率によって変動するため、下記6章の指示文をClaude Codeに投入し、実コードベースを踏まえた精緻な再算出を行うことを推奨します。

| フェーズ | 残作業内容 | 暫定見積もり(AI支援開発想定) |
|---|---|---|
| フェーズ1仕上げ | 画像差し替え、variant確認 | 0.5〜1時間 |
| フェーズ2 | バックエンド一式(振り分け・計測・DB・LLM連携・結合テスト) | 15〜25時間 |
| フェーズ3 | DB設計・蓄積運用・自動分析バッチ | 10〜15時間(+運用として案件データが溜まるまでの期間は別途) |

**週あたりの稼働可能時間が未確定のため、完成予定日は算出していません。** 6章の指示文で、Claude Codeに残タスクの精査と、想定稼働時間(例: 週5時間など)を踏まえた完成予定日の算出・本計画書への反映を依頼してください。

---

## 6. Claude Codeへの指示文(稼働時間算出・計画書更新用)

以下をそのままClaude Codeのターミナルに貼り付けてください。

```
このリポジトリ(lp-cvr-tool)の開発計画書 lp-cvr-tool-project-plan.md をルート直下に配置します(まだ配置していない場合はアップロードするのでファイルを追加してください)。

以下の作業をお願いします。

## 1. 現状の実装状況を精査
- リポジトリ内の既存ファイル・ディレクトリ構成を確認し、計画書に記載されているフェーズ1〜3の各タスクについて、実際に完了しているか、部分的か、未着手かを再確認してください
- 計画書の「進捗ステータス」列に、必要であれば修正を加えてください

## 2. 残作業の洗い出しと工数見積もり
- フェーズ2・フェーズ3それぞれについて、未着手のタスクを実装単位まで具体的に分解してください(例: 「assign-variant.ts実装」は「Cookie発行ロジック」「振り分け比率のロジック」「エラーハンドリング」などに分解)
- 分解した各タスクについて、Claude Codeとの協働作業を前提とした想定所要時間(時間単位)を見積もってください
- 見積もりの前提条件(1タスクあたりのレビュー・修正往復回数、テスト工数を含むかどうかなど)を明記してください

## 3. 完成予定日の算出
- 私(ユーザー)が本プロジェクトに充てられる週あたりの稼働時間を確認してください(不明な場合は質問してください)
- 確認した稼働時間をもとに、フェーズ2完了予定日、フェーズ3完了予定日(初期実装完了ベース。データ蓄積期間は除く)を算出してください
- 算出根拠(合計見積もり時間 ÷ 週あたり稼働時間 = 週数)を明示してください

## 4. 計画書の更新
- 上記1〜3の内容を lp-cvr-tool-project-plan.md に反映し、以下を明確にしてください
  - 各タスクの詳細な進捗ステータス
  - フェーズ2・フェーズ3の詳細工数見積もり(タスク別内訳つき)
  - 算出した完成予定日とその根拠
  - 次に着手すべき直近アクション(優先順位付き)
- 更新後、変更差分の要約を報告してください

質問がある場合(稼働時間の確認など)は、作業を進める前に先に質問してください。
```

---

## 7. 更新履歴

| 日付 | 内容 |
|---|---|
| 2026-08-01 | 初版作成(フェーズ1完了間近の状態を反映) |
| 2026-08-01 | git履歴との突き合わせにより修正。1-7のステータス表現を訂正(疎通確認と目視確認を区別)、記載漏れだった1-10(Formspree CTA接続)・1-11(GoatCounterアクセス解析)を追加、フェーズ1進捗率を95%に更新、リポジトリルートに本ファイルを配置 |
| 2026-08-06 | `.claude/docs/note.md`との突き合わせにより修正。1-7のvariant-2目視確認を完了に更新、1-12(本番稼働最終検証)・1-13(X投稿による初回配布)を追加、フェーズ1進捗率を97%に更新、次のステップに配布フォローアップ2件を追加 |
| 2026-09-11 | フェーズ2着手。実行環境をCloudflare Workersに確定(コスト比較の結果)、DBはSupabase→D1へ置き換え検討中。`workers/`一式を新設し2-1〜2-3を実装・ビルド検証まで完了、進捗率0%→20%に更新 |
| 2026-09-11 | DBをD1に確定。Cloudflareアカウント新規作成・wrangler login・D1データベース作成(`lp-cvr-tool-db`)・スキーマ適用・`wrangler deploy`まで完了し、`/assign`・`/track`を実機スモークテストで確認。進捗率20%→45%に更新 |
| 2026-09-11 | `scripts/build.js`にトラッキングスクリプトを組み込み(view自動送信・CTA送信成功時にcv送信)、3variant再生成。コミット・push→GitHub Actions自動デプロイ後、本番LP(variant-2)をブラウザで開きD1にviewイベントが記録されることを実機確認。進捗率45%→60%に更新 |
| 2026-09-11 | 配布リンクの独自ドメイン化(`lp.kotobuki.shop`等)を検討したが、本番サイトのDNS変更を伴う手間を避けるためユーザー判断で見送り。`workers.dev`のURL(`https://lp-cvr-tool.lp-cvr-tool-workers.workers.dev/assign?project=project-a`)をそのまま配布に使う方針に確定。進捗率60%→65%に更新 |
| 2026-09-11 | 統計判定ロジック(2-7)実装。`workers/src/stats.ts`にBeta-Binomial+モンテカルロでprobabilityBest・95%信用区間・勝者判定を実装、`GET /stats`として公開。合成データ(variant-1のみCVR30%、他10%程度)を一時投入し`winner: "variant-1"`を正しく検出することを確認、検証後に合成データは削除。進捗率65%→80%に更新 |

# 環境変数・秘匿情報
.env
.env.*
!.env.example

# 依存関係(フェーズ2以降でnpmパッケージを導入した場合用)
node_modules/

# OS生成ファイル
.DS_Store
Thumbs.db

# ビルド出力
dist/

# Cloudflare Workers(wranglerのローカルキャッシュ・アカウント情報)
workers/.wrangler/
workers/*.local

# ヒーロー画像の生成候補(選定前のドラフト、確定分のみassets/imgへ手動で昇格)
assets/hero-candidates/

# LPコピーの生成候補(選定前のドラフト、確定分のみbuild.jsのvariantsへ手動で反映)
assets/copy-candidates/

# エディタ設定
.vscode/
.idea/

# ローカル作業メモ(社外に出さない検討事項・進捗ログ)
.claude/
